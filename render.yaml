services:
  - type: web
    name: i9-fit-gym-voice
    env: static
    buildCommand: npm run build
    publishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
